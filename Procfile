# Fallback launch command. Under the committed manifest.yml,
# `command: ./bin/server` takes precedence — this line matters only for
# bare `cf push` runs that don't use the manifest.
web: ./bin/server
